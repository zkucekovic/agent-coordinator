# scripts/

Utility scripts for working with the coordinator.

| Script | Purpose |
|---|---|
| `parse_next.sh` | Extracts the `NEXT:` field from the latest handoff block (useful for shell automation) |
